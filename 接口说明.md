# 接口说明

## 1. 位置

Pynq 客户端 <-接口-> 服务端 <-> LLM

## 2. 交互

客户端上行：

- 原始音频分片
- 静态图片
- 温湿度数据
- 宠物性格配置 id

服务端下行：

- 当前会话使用的配置 id
- 流式文本
- 情绪、动作、会话状态

双向：

- 行为事件
- 配置信息

## 3. 通信架构

- 一条长连接 WebSocket 作为会话事件与实时媒体控制主通道
- 少量 HTTP 接口负责配置与资源型操作

## 4. 传输模型

### 4.1 HTTP

HTTP 用于：

- 列出可用配置
- 查询配置元数据
- 健康检查与能力探测

### 4.2 WebSocket

WebSocket 用于：

- 会话初始化
- 实时音频上传
- 可选图像上传
- 传感器数据上传
- 双向行为事件
- 对话生命周期事件
- 文本响应下发
- 状态、错误与心跳事件

## 5. 基础约定

### 5.1 版本

所有公开接口统一采用 `v1` 版本。

- HTTP 基础路径：`/api/v1`
- WebSocket 端点：`/api/v1/pet/ws`

### 5.2 内容类型

- JSON：`application/json`
- multipart 上传：`multipart/form-data`

### 5.3 时间

- 所有时间戳统一使用 ISO 8601 UTC 字符串
- 示例：`2026-05-26T08:15:30.123Z`

### 5.4 标识符

- `device_id`：设备稳定硬件标识
- `session_id`：服务端生成的单次 WebSocket 会话标识
- `request_id`：客户端生成的请求关联标识
- `config_id`：服务端生成的配置资源标识
- `trace_id`：可选服务端诊断跟踪标识

### 5.5 通用消息包络

所有 WebSocket JSON 消息统一采用如下外层结构：

```json
{
  "type": "string",
  "version": "v1",
  "request_id": "optional-client-request-id",
  "session_id": "optional-session-id",
  "timestamp": "2026-05-26T08:15:30.123Z",
  "payload": {}
}
```

规则：

- `type` 必填
- `version` 必填，且必须为 `v1`
- `payload` 必填，且必须为对象
- 当客户端需要关联请求与响应时，`request_id` 必填
- 会话建立后，`session_id` 必填

前向兼容规则：

- 接收方必须忽略 `payload` 中未知字段
- 接收方必须忽略顶层包络中除必填字段外的未知字段
- 接收方可以对未知 `type` 返回 `INVALID_MESSAGE`
- 删除 `v1` 中的必填字段属于破坏性变更
- 新增可选字段不属于破坏性变更

## 6. HTTP 接口

## 6.1 `GET /api/v1/health`

用途：

- 存活检查
- 基础依赖状态检查

响应示例：

```json
{
  "status": "ok",
  "service": "pet-gateway",
  "version": "v1",
  "open_llm_vtuber": {
    "reachable": true
  },
  "time": "2026-05-26T08:15:30.123Z"
}
```

## 6.2 `GET /api/v1/capabilities`

用途：

- 告知客户端当前服务端能力开关

响应示例：

```json
{
  "protocol_version": "v1",
  "features": {
    "streaming_audio_upload": true,
    "image_upload": true,
    "sensor_upload": true,
    "bidirectional_behavior_events": true,
    "local_tts_expected": true
  },
  "limits": {
    "max_image_bytes": xxxxx,
    "max_ws_message_bytes": xxxxx,
    "audio_chunk_ms_recommended": xxx
  }
}
```

## 6.3 `GET /api/v1/configs`

用途：

- 列出服务端已存储宠物性格配置

响应示例：

```json
{
  "items": [
    {
      "config_id": "cfg_01JV7H7K2Q0A9S2E5A8N4K1M3P",
      "name": "pet-default-cn",
      "version": 1,
      "status": "ready",
      "is_default": true,
      "created_at": "2026-05-26T08:15:30.123Z"
    }
  ]
}
```

## 6.4 `GET /api/v1/configs/{config_id}`

用途：

- 获取单个宠物性格配置的元数据

响应示例：

```json
{
  "config_id": "cfg_01JV7H7K2Q0A9S2E5A8N4K1M3P",
  "name": "pet-default-cn",
  "version": 1,
  "is_default": true,
  "created_at": "2026-05-26T08:15:30.123Z",
  "source_type": "yaml",
  "validation": {
    "ok": true,
    "warnings": []
  }
}
```

## 7. WebSocket 接口

端点：

- `GET /api/v1/pet/ws`

服务端每个连接设备实例接受一个 WebSocket 会话。重连会生成新的 `session_id`。

### 7.1 连接生命周期

1. 客户端建立 WebSocket 连接
2. 客户端发送 `session.init`
3. 服务端校验配置选择和会话元数据
4. 服务端响应 `session.ready`
5. 客户端开始上传音频、图像、传感器和行为事件
6. 服务端持续下发对话状态与响应事件
7. 任意一方都可以发送 `session.close`

### 7.2 客户端到服务端事件

#### 7.2.1 `session.init`

用途：

- 初始化会话
- 声明设备身份与所选宠物性格配置

载荷：

```json
{
  "device_id": "pynq-pet-001",
  "device_name": "xxx",
  "config_id": "cfg_01JV7H7K2Q0A9S2E5A8N4K1M3P",
  "client_capabilities": {
    "local_tts": true,
    "camera": true,
    "temperature_sensor": true,
    "humidity_sensor": true,
    "behavior_executor": true
  },
  "audio_format": {
    "codec": "pcm_s16le",
    "sample_rate": 16000,
    "channels": 1,
    "frame_ms": 96
  }
}
```

规则：

- 推荐携带 `config_id`
- 如果未携带 `config_id`，服务端使用默认配置
- 如果未携带 `config_id` 且不存在默认配置，服务端必须以 `INVALID_CONFIG_ID` 拒绝会话
- 如果显式提供了 `config_id`，其优先级高于默认配置

#### 7.2.2 `audio.chunk`

用途：

- 向服务端流式上传麦克风原始音频

载荷：

```json
{
  "seq": 12,
  "audio_base64": "BASE64_PCM_BYTES",
  "codec": "pcm_s16le",
  "sample_rate": 16000,
  "channels": 1,
  "frame_ms": 96
}
```

规则：

- `seq` 必须在单会话内单调递增
- `audio_base64` 包含原始 PCM 字节，不是 WAV 容器
- `sample_rate` 必须为 16000
- `channels` 必须为 1
- `frame_ms` 最好为 32ms 整数倍

截断规则：

- 服务端复用上游 VAD ，客户端无需处理截断

#### 7.2.3 `image.upload`

用途：

- 向服务端上传一张静态图像

载荷：

```json
{
  "image_id": "img_001",
  "source": "camera",
  "mime_type": "image/jpeg",
  "data_base64": "BASE64_IMAGE_BYTES",
  "width": 320,
  "height": 240,
  "sampled_at": "2026-05-26T08:15:30.123Z"
}
```

规则：

- 图片注入只发生一次，不会重复注入
- 如果上一张图未被消费又上传新图，则新图覆盖旧图
- 超过尺寸限制的图片必须以 `IMAGE_TOO_LARGE` 拒绝

#### 7.2.4 `sensor.report`

用途：

- 上报温度与湿度数据

载荷：

```json
{
  "temperature_c": xxx,
  "humidity_pct": xxx,
  "sampled_at": "2026-05-26T08:15:30.123Z"
}
```

规则：

- 信息型事件
- 可以影响模型提示词或行为选择
- v1 中它本身不能单独触发一轮对话

#### 7.2.5 `behavior.event`

用途：

- 上报客户端侧行为事件或执行状态

载荷：

```json
{
  "related_command_id": "cmd_001",
  "event": "xxx",
  "status": "success",
  "duration_ms": 1200,
  "end_at": "2026-05-26T08:15:30.123Z"
}
```

v1 建议事件类别：

- 行为执行开始
- 行为执行完成
- 行为执行失败
- 本地空闲状态变化

#### 7.2.6 `client.command`

用途：

- 客户端向服务端发送明确语义指令, 由服务端决定是否触发对话、行为、模式切换或系统动作

载荷：

```json
{
  "command": "wake_up",
}
```

#### 7.2.7 `text.input`

用途：

- 直接发送文本输入，不经过音频

载荷：

```json
{
  "text": "xxx"
}
```

#### 7.2.8 `conversation.interrupt`

用途：

- 中断当前服务端正在进行的响应

载荷：

```json
{
  "reason": "xxx"
}
```

- 该事件自然映射到 Open-LLM-VTuber 现有的中断路径
- 客户端不设计语音中断，语音中断由服务端上游 VAD 处理

#### 7.2.9 `client.tts.started`

用途：

- 通知服务端客户端已开始本地播放

载荷：

```json
{
  "response_id": "rsp_001"
}
```

#### 7.2.10 `client.tts.finished`

用途：

- 通知服务端客户端已完成本地播放

载荷：

```json
{
  "response_id": "rsp_001"
}
```

该事件是对当前前端风格播放完成信号的替代

#### 7.2.11 `heartbeat`

用途：

- 保持连接活跃

载荷：

```json
{
  "uptime_ms": 183920
}
```

建议频率：

- 每 `15s` 一次

#### 7.2.12 `session.close`

用途：

- 主动关闭会话

载荷：

```json
{
  "reason": "client_shutdown"
}
```

### 8.3 服务端到客户端事件

#### 8.3.1 `session.ready`

用途：

- 确认会话已准备就绪

载荷：

```json
{
  "session_id": "ses_01JV7HSMQ32F4D9D6D0W0K8Q5W",
  "device_id": "pynq-pet-001",
  "config_id": "cfg_01JV7H7K2Q0A9S2E5A8N4K1M3P",
  "server_capabilities": {
    "vad": true,
    "asr": true,
    "vision_context": true,
    "behavior_planning": true
  }
}
```

#### 8.3.2 `conversation.started`

用途：

- 表示一轮对话开始处理

载荷：

```json
{
  "turn_id": "turn_001",
  "trigger": "audio"
}
```

#### 8.3.3 `response.text`

用途：

- 下发主响应文本，供本地 TTS 使用

载荷：

```json
{
  "response_id": "rsp_001",
  "turn_id": "turn_001",
  "text": "The weather is nice today, and the room temperature is comfortable.",
  "voice": "happy"
}
```

规则：

- 服务端可以在一轮中发送多个 `response.text`
- 客户端需要按顺序拼接
- `voice` 为本地 TTS 提供音量音调设定
- `response.complete` 是唯一权威的响应结束信号

#### 8.3.4 `response.behavior`

用途：

- 指示客户端执行硬件或动画行为

载荷：

```json
{
  "response_id": "rsp_001",
  "command_id": "cmd_001",
  "command": "xxx",
  "args": {}
}
```

行为命令集合见 [Command 设计](./LLM本地控制Command设计.md)

**待定**

#### 8.3.5 `response.complete`

用途：

- 表示当前轮次的文本与动作建议已经全部发送完毕

载荷：

```json
{
  "response_id": "rsp_001",
  "turn_id": "turn_001"
}
```

仅表示服务端发送完毕

#### 8.3.6 `conversation.interrupted_ack`

用途：

- 向客户端回复确认当前轮次已被中断

载荷：

```json
{
  "turn_id": "turn_001",
  "reason": "user_speaking_again"
}
```

#### 8.3.7 `status.update`

用途：

- 通知非错误类运行状态

载荷：

```json
{
  "stage": "thinking",
}
```

#### 8.3.8 `heartbeat.ack`

用途：

- 确认客户端心跳

载荷：

```json
{
  "ok": true
}
```

#### 8.3.9 `error`

用途：

- 返回可恢复或终止类错误

载荷：

```json
{
  "code": "INVALID_CONFIG_ID",
  "message": "Selected config does not exist",
  "retryable": false,
  "request_id": "req_001",
  "turn_id": "turn_001",
  "field": "config_id",
  "trace_id": "trc_001"
}
```

错误类型定义见 [错误模型](#err)

#### 8.3.10 `session.closed`

用途：

- 表示服务端正在关闭会话

载荷：

```json
{
  "reason": "server_shutdown"
}
```

## 9. 会话状态机

### 9.1 状态

- `disconnected`
- `connected`
- `ready`
- `listening`
- `processing`
- `responding`
- `waiting_client_playback`
- `idle`
- `closing`

### 9.2 状态流转

1. disconnected -> connected
   - WebSocket 握手成功

2. connected -> ready
   - `session.init` 被接受
   - 服务端发出 `session.ready`

3. ready -> listening
   - 客户端开始流式发送音频

4. listening -> processing
   - 服务端根据上游 VAD 确认触发轮次

5. processing -> responding
   - 服务端开始产出响应

6. responding -> waiting_client_playback
   - 服务端发出 `response.complete`

7. waiting_client_playback -> idle
   - 客户端发出 `client.tts.finished`

8. 任何状态 -> processing
   - 中断后立即开始下一轮

9. 任何状态 -> closing
   - `session.close` 或不可恢复错误

10. ready -> processing
   - 客户端发送 `text.input`

11. idle -> processing
   - 客户端发送 `text.input`

12. idle -> listening
   - 客户端重新开始上传音频流

### 9.3 轮次并发规则

- 一个会话任一时刻最多只能有一个服务端活跃轮次
- 当轮次处于 `processing` 时，新的 `text.input` 必须被服务端以 `TURN_ALREADY_ACTIVE` 拒绝，除非客户端先中断
- 当轮次处于 `responding` 或 `waiting_client_playback` 时，新检测到的用户语音应被服务端视作中断尝试
- 服务端对某轮发出 `conversation.interrupted_ack` 后，不得再为该轮继续发出新的 `response.text`、`response.behavior`、`response.expression` 或 `response.complete`
- 如果客户端在中断生效后立刻发起替代轮次，服务端应先发 `conversation.interrupted_ack`，再发新轮次的 `conversation.started`

## 10. 错误模型<span id="err"></span>

v1 错误码：

- `INVALID_MESSAGE`
- `UNSUPPORTED_VERSION`
- `INVALID_CONFIG_ID`
- `INVALID_CLIENT_COMMAND`
- `CONFIG_VALIDATION_FAILED`
- `AUDIO_FORMAT_UNSUPPORTED`
- `AUDIO_CHUNK_OUT_OF_ORDER`
- `IMAGE_TOO_LARGE`
- `PAYLOAD_TOO_LARGE`
- `SESSION_NOT_READY`
- `TURN_ALREADY_ACTIVE`
- `INTERRUPT_WITHOUT_ACTIVE_TURN`
- `UPSTREAM_ASR_ERROR`
- `UPSTREAM_AGENT_ERROR`
- `UPSTREAM_TIMEOUT`
- `INTERNAL_ERROR`

规则：

- 非法 JSON 直接关闭 WebSocket
- 语义级请求错误尽量返回 `error` 事件并保持会话存活
- 上游运行时失败只有在客户端确实可以安全重试时才应设置 `retryable=true`
- 如果错误由某个客户端事件引发，服务端应在有 `request_id` 时回传该 `request_id`
- 如果错误与某轮处理相关，服务端应在可获得时回传 `turn_id`

## 11. 校验规则

### 11.1 音频

- 采样率必须为 `16000`
- 声道数必须为 `1`
- 编码必须为 `pcm_s16le`
- 拒绝超过服务端配置大小上限的音频块

### 11.2 图像

- 允许 MIME 类型：
  - `image/jpeg`
  - `image/png`
- 拒绝不支持类型
- 拒绝尺寸超过 320 * 240 帧

### 11.3 控制命令

- 客户端上报的未知事件服务端应接收，可静默忽略
- 客户端上报的未知命令服务端应接收，并通过 `error` 回报
- 服务端下发的未知命令客户端应接收，并通过失败 `behavior.event` 回报
- 当客户端回报某个服务端命令的执行结果时，应携带 `related_command_id`
